<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.deliverybagual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".InformacoesPedido"></activity>
        <activity android:name=".EscolherChurrascaria" />
        <activity android:name=".Confirmacao" />
        <activity android:name=".AgendarEntrega" />
        <activity android:name=".AdicionarItemPorTamanho" />
        <activity android:name=".Carrinho" />
        <activity android:name=".AdicionarItemPorTipo" />
        <activity android:name=".AdicionarItem" />
        <activity android:name=".ResumoPedido" />
        <activity android:name=".ListaPedidos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>